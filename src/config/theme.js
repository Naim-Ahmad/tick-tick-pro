import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4772fa",
    },
    secondary: {
      main: "#F4B033",
    },
  },
});

export default theme;
